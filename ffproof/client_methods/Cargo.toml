[package]
name = "encrypted-spaces-client-methods"
version = "0.1.0"
edition = "2021"

[build-dependencies]
# Not using workspace deps: this crate isn't a workspace member.
risc0-build = "3.0.5"

[package.metadata.risc0]
methods = ["guest"]
